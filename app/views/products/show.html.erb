<!-- <% content_for(:title, "Product | Reviews") %> -->
<div class="col-md-6">
    <h3><%= @product.name %></h3>
    <p>Country of Origin: <%= @product.country_of_origin%></p>
    <p>Price per bushel: $<%= @product.cost%></p>

    <%= link_to "Edit", edit_product_path(@product) %>
    <%= link_to "Delete", product_path(@product),
    :data => {:confirm => "are you sure?", :method => "delete"} %>
    <%= link_to "Main page", products_path %>
</div>

<div class="col-md-6">
  <% if @product.reviews.any? %>
    <h6>Recent reviews for this product:</h6>
    <%= @product.reviews.each do |review| %>
    <li style="list-style: none;">
      <div class="well">
          <p><%= review.content_body %></p>
          <p><%= review.author %></p>
          <p>Rating: <%= review.rating %></p>
  </div>
    </li>

    <% end %>
  <% end %>
